/**
 * Where this project lives.
 *
 * The same repository the updater is pinned to, kept here so the header link
 * and the admin console cannot drift apart from it. The server sends its own
 * copy in the update status; this is the value used before that has loaded.
 */
export const PROJECT_URL = 'https://github.com/ZcarecroW/devcolorz'
